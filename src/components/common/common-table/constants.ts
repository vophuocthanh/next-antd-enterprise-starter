export const DEFAULT_SORT_DIRECTIONS = ['ascend', 'descend', null] as const;

export const EMPTY_TEXT_CONFIG = {
  minHeight: 550,
  message: 'Không có dữ liệu.',
} as const;

export const PAGINATION_LOCALE = {
  items_per_page: ' / trang',
} as const;
